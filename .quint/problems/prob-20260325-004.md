---
id: prob-20260325-004
kind: ProblemCard
version: 1
status: active
title: Safety Hardening — Wire validate_tool_call + fail-fast injection abort
mode: tactical
created_at: 2026-03-25T20:10:29Z
updated_at: 2026-03-25T20:10:30Z
---

# Safety Hardening — Wire validate_tool_call + fail-fast injection abort

## Signal

3 concrete gaps confirmed by code inspection: (1) validate_tool_call() is dead code — written in safety.py:123-130, never imported/called in agent.py; path traversal in tool args goes unchecked. (2) No circuit breaker — injection warnings log and prepend a header but never abort; agent can process unlimited injected files. (3) Security outcome is advisory — agent relies on LLM calling OUTCOME_DENIED_SECURITY; nothing enforces it deterministically.

## Constraints

- No new dependencies
- safety.py API unchanged
- ~15 net new lines across 2 files
- All existing tests must pass

## Optimization Targets

- Deterministic injection abort — Python layer stops task before LLM processes injected content
- Path traversal blocked at dispatch time — validate_tool_call wired before _dispatch
- Security outcome enforced by code — not advisory to LLM

## Acceptance

validate_tool_call imported and called before _dispatch; injection scan aborts task with OUTCOME_DENIED_SECURITY instead of prepending header; 3 new tests pass; all existing tests pass; ruff clean

## Blast Radius

agent.py only — safety.py validate_tool_call already correct; tests/test_agent.py gets 3 new cases

## Reversibility

high

## Related History

- [DecisionRecord] **Targeted unit tests for 4 missing agent.py control paths** `dec-20260325-001`
- [ProblemCard] **agent.py core loop has zero test coverage** `prob-20260324-008`
- [DecisionRecord] **A: MagicMock(spec=...)** `dec-20260324-002`
